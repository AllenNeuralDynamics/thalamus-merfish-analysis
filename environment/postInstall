#!/usr/bin/env bash
set -e
if [ -n "$DOTFILES_REPO" ]; then
    sh -c "$(curl -fsLS get.chezmoi.io)" -- init --apply $DOTFILES_REPO -b /usr/bin
    # to manage in vscode:
    ln -s .chezmoi ~/.local/share/chezmoi/
fi